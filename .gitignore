__pycache__/
*.py[cod]

# Reports are derived data, regenerated by the build-and-deploy workflow and
# served straight to Pages. Running a generator locally writes here; nothing
# under either path belongs in git.
reports/
_site/
